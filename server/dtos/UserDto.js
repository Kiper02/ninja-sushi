export default class UserDto {
    email;
    id;
    isActivated;

    constructor(model) {
        this.email = this.email;
        this.id = model.id;
        this.isActivated = model.isActivated;
    }
} 